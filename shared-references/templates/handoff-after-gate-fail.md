## Turn ended: consecutive gate failures

**Failing gate**: `{{FAILING_LABEL}}`
**Log**: `.ralph/gates/{{FAILING_LABEL}}-latest.log`
**Consecutive failures**: {{CONSECUTIVE_FAILURES}}

The loop ended this turn after {{CONSECUTIVE_FAILURES}} consecutive gate
failures without a passing run. The next turn will start with
troubleshooting guidance injected.
