---
description: Update the ralph-wiggum-plugin's internal Spec Kit integration files when a new Spec Kit version is released.
argument-hint: "[VERSION]  e.g. 0.8.6  (optional context — omit to infer from staged diff or release notes)"
---

Invoke the `ralph-plugin-speckit-update` skill to bring the plugin's Spec Kit integration in sync with the latest release.

$ARGUMENTS
