---
description: Sync this project's Spec Kit integration to the latest tagged release, or to a specific version if provided. Updates the specify-cli binary and reruns specify integration upgrade. Does not commit.
argument-hint: "[VERSION]  e.g. 0.8.5 or v0.8.6  (omit for latest)"
---

Invoke the `spec-kit-version-sync` skill to upgrade Spec Kit in this project.

$ARGUMENTS
